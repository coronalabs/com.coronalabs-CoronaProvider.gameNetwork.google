Sources for the plugin `CoronaProvider.gameNetwork.google`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "CoronaProvider.gameNetwork.google" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
